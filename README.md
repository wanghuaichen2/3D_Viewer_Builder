# 📁 3D模型查看器生成器（支持PLY/STL/OBJ）

基于Three.js的离线3D模型查看器生成工具。可以同时拖入最多5个PLY/STL/OBJ模型文件，点击按钮即可生成一个**独立的HTML文件**，该文件包含完整的3D查看器，支持按文件名控制模型的显示/隐藏，方便分享或离线查看。

![主界面截图](images/main-screenshot.png)  
*（请替换为你的截图）*

---

## ✨ 功能特点

- **多格式支持**：PLY、STL、OBJ（二进制或ASCII）
- **批量处理**：最多同时处理5个模型文件
- **实时统计**：加载PLY文件时自动解析顶点数和面数（STL/OBJ暂未解析）
- **一键生成**：生成一个包含所有模型数据的`view.html`文件，完全离线可用
- **模型控制**：生成的查看器左侧提供复选框，可独立控制每个模型的显示/隐藏
- **友好交互**：左键旋转、右键平移、滚轮缩放，操作直观

---

## 🚀 快速使用

1. **打开工具**  
   在浏览器中打开`main_v2.2.html`。
2. **拖拽或点击选择模型文件**  
   支持`.ply`、`.stl`、`.obj`格式，最多可选5个文件。  
4. **点击“生成 view.html”**  
   等待进度条完成，浏览器会自动下载一个名为`view.html`的文件。
5. **打开生成的查看器**  
   用浏览器打开`view.html`，即可看到所有模型，左侧复选框可控制显隐，右侧调试面板显示加载日志。  

---

## 📂 支持的文件格式

| 格式 | 说明 |
|------|------|
| **PLY** | 支持ASCII和二进制，自动解析顶点/面数 |
| **STL** | 支持ASCII和二进制，统计信息暂未解析 |
| **OBJ** | 支持ASCII，统计信息暂未解析 |

> ⚠️ 注意：生成器会将模型文件内嵌到`view.html`中，因此如果模型文件很大，生成的HTML也会很大，建议单个文件不超过50MB。

---

## 🧩 生成的查看器功能

- **模型控制**：左侧面板列出所有模型文件名，勾选/取消即可显示/隐藏对应模型。
- **调试信息**：右侧面板显示加载过程中的日志，包括模型总数、解码状态、错误信息等。
- **自适应相机**：所有模型加载完成后自动调整相机位置，使模型居中显示。
- **交互方式**：
  - 左键：旋转
  - 右键：平移
  - 滚轮：缩放
- **半透明材质**：每个模型被赋予随机颜色的半透明材质，方便观察重叠部分。

---

## ⚙️ 技术细节

- **前端**：原生HTML/CSS/JavaScript，无任何构建步骤。
- **3D引擎**：Three.js (r128) + 官方加载器（PLYLoader、STLLoader、OBJLoader、OrbitControls）。
- **数据嵌入**：模型文件数据通过Base64编码内嵌到生成的HTML中，确保单文件离线可用。

---

## 📸 截图占位

请将以下截图放入`images/`文件夹并替换链接：

- `images/main-screenshot.png` – 主界面截图
- `images/dropzone.png` – 拖拽区域截图
- `images/filelist.png` – 文件列表截图
- `images/viewer.png` – 生成的查看器截图

---

## 🤝 贡献指南

欢迎提交Issue或PR！如果你想改进此工具，请遵循以下步骤：

1. Fork本仓库
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的修改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开一个Pull Request

---

## 📄 许可证

本项目采用 MIT 许可证 – 详情请参见 [LICENSE](LICENSE) 文件。

---

## 🙏 致谢

- [Three.js](https://threejs.org/) – 强大的3D库
- 所有贡献者和用户

---

**如果这个工具对你有帮助，请给一个⭐吧！**